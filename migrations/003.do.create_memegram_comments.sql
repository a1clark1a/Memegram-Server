CREATE TABLE memes_comments (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    comment TEXT NOT NULL,
    date_created TIMESTAMP DEFAULT now() NOT NULL,
    memes_id INTEGER REFERENCES memes_tables(id) ON DELETE CASCADE NOT NULL,
    user_id INTEGER REFERENCES memes_users(id) ON DELETE CASCADE NOT NULL
);